# modules/nixos/desktop/urxvt.nix

{ pkgs, ... }:

{
  # 3. Optional: Make it your default terminal in i3
	#services.xserver.windowManager.i3.config.keybindings = {
  	#  "$mod+Return" = "exec urxvt";
  	#};
}

